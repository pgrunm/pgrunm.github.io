# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  default:
    cmds:
      - cmd: hugo serve -b "http://localhost:1313"
        ignore_error: true
      - defer: rm .hugo_build.lock
